class EmployeeDomain {
  final int id;
  final String name;
  final String role;
  final DateTime joinDate;
  final DateTime? leaveDate;
  EmployeeDomain(this.id, this.name, this.role, this.joinDate, this.leaveDate);
}
